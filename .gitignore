# Dependencies
node_modules/

# Local env / secrets
.env
.env.*
!.env.example

# Data (SQLite + WAL/SHM sidecars)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Logs
logs/
*.log
npm-debug.log*
pino-*.log

# PM2 runtime
.pm2/
ecosystem.config.local.cjs

# Claude Code: skills are local tooling; per-machine local settings file
.claude/skills/
.claude/settings.local.json

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Provider key material
keys/
*.pem
*.key
!**/_template.key

# Capture-side runtime config — account UUIDs and laptop secrets
# (DATASCRAPER_URL, INTERNAL_TOKEN). Templates live in capture/*.example.*
capture/tenants.json
capture/.env
